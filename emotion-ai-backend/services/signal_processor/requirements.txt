# Silero VAD is NOT a pip package — it is loaded via torch.hub at runtime.
# torch.hub.load('snakers4/silero-vad', ...) handles download automatically.
torch>=2.1.0
torchaudio>=2.1.0
librosa>=0.10.0
noisereduce>=3.0.0
numpy>=1.24.0
onnxruntime>=1.17.0
soundfile>=0.12.0
aiohttp>=3.9.0
python-dotenv>=1.0.0
pydantic>=2.0.0
